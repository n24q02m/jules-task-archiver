{
  "name": "jules-task-archiver-tests",
  "version": "2.1.1",
  "description": "Tests for Jules Task Archiver",
  "main": "background.js",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40"
  }
}
