{
  "name": "jules-task-archiver-tests",
  "version": "2.1.1",
  "description": "Tests for Jules Task Archiver",
  "main": "background.js",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "jsdom": "^29.0.1"
  }
}
